<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Add comment</title>
</head>
<body>
{% if method %}
<h1>Modify Comment</h1>

{% else %}
<h1>Create a Comment</h1>
{% endif %}
<form action="/post/{{postId}}/comment/{{comment.id}}" method="POST">

    <textarea name="content" placeholder="Enter your comment" >{{ comment.content if comment else '' }}</textarea>
    <button type="submit">Submit</button>
</form>
<button type="submit"><a href="/">Back</a></button>
</body>