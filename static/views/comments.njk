<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments - Veterinary Social Network</title>
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/components.css">
    <link rel="stylesheet" href="/css/layout.css">
    <script src="/js/theme.js" defer></script>
</head>
<body>
    <div class="container">
        <header class="header">
            <div class="header-content">
                <h1 class="navbar-brand">VetNet</h1>
                <nav class="header-nav">
                    <button class="theme-toggle" aria-label="Toggle theme">
                        <span class="theme-toggle-icon sun">☀️</span>
                        <span class="theme-toggle-icon moon">🌙</span>
                    </button>
                    <a href="/" class="btn btn-secondary">Back to Posts</a>
                </nav>
            </div>
        </header>

        <main class="main-content">
            <div class="comments-section">
                <h2>Comments</h2>
                
                {% if comments.length === 0 %}
                    <div class="text-center">
                        <p class="alert alert-info">No comments yet. Be the first to comment!</p>
                    </div>
                {% else %}
                    {% for comment in comments %}
                        <div class="comment-card">
                            <div class="comment-header">
                                <span class="comment-author">{{ comment.author.name }}</span>
                                <span class="comment-date">{{ comment.createdAt.toDateString() }} at {{ comment.createdAt.getHours() }}:{{ comment.createdAt.getMinutes() }}</span>
                            </div>
                            <div class="comment-content">{{ comment.content }}</div>
                            
                            {% if auth %}
                                {% if userId == comment.authorId or role == "ADMIN" %}
                                    <div class="comment-actions">
                                        <a href="/post/{{ comment.postId }}/comment/{{ comment.id }}" class="btn btn-sm btn-warning">Edit</a>
                                        <form action="/post/{{ comment.postId }}/comment/{{ comment.id }}" method="post" style="display: inline;">
                                            <input type="hidden" name="_method" value="DELETE">
                                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this comment?')">Delete</button>
                                        </form>
                                    </div>
                                {% endif %}
                            {% endif %}
                        </div>
                    {% endfor %}
                {% endif %}
                
                {% if auth %}
                    <div class="text-center mt-4">
                        <a href="/post/{{ postId }}/comment" class="btn btn-primary">Add New Comment</a>
                    </div>
                {% endif %}
            </div>
        </main>
    </div>
</body>
</html>